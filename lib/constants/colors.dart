import 'package:flutter/material.dart';

class AppColor{
  static const primary = Color(0xFFFF2D55);
  static const textLight = Color(0xFF858585);
  static const appbar = Color(0xFFF6F6F6);
  static const shadow = Color(0xFFFBA83C);
  static const divider = Color(0xFFDADADA);
  static const gradient = LinearGradient(colors: [AppColor.primary,AppColor.shadow]);
}

